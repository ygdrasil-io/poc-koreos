/**
 * Squelette du module koreos-appkit.
 *
 * Ce fichier est un placeholder qui sera remplacé par l'implémentation
 * AppKit (NSWindow, NSApplication, NSView CAMetalLayer) dans les tickets
 * dédiés du jalon M1.
 * Il garantit qu'aucun avertissement "no source roots" n'est émis par Gradle.
 *
 * Cible : jvm (JDK 25, Foreign Function & Memory API).
 */
package io.ygdrasil.koreos.appkit

// TODO M1 : EventLoop (actual jvmMain) via kextract + AppKit FFM
// TODO M1 : KoreosApplication, KoreosAppDelegate, KoreosWindowDelegate (subclasses Obj-C)
// TODO M1 : NSWindow wrappé dans une implémentation de koreos-core Window
