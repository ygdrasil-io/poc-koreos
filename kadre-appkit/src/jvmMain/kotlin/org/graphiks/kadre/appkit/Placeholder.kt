/**
 * Skeleton of the kadre-appkit module.
 *
 * This file is a placeholder that will be replaced by the AppKit
 * implementation (NSWindow, NSApplication, NSView CAMetalLayer) in the
 * dedicated tickets of the M1 milestone.
 * It guarantees that no "no source roots" warning is emitted by Gradle.
 *
 * Target: jvm (JDK 25, Foreign Function & Memory API).
 */
package org.graphiks.kadre.appkit

// TODO M1: EventLoop (actual jvmMain) via kextract + AppKit FFM
// TODO M1: KadreApplication, KadreAppDelegate, KadreWindowDelegate (Obj-C subclasses)
// TODO M1: NSWindow wrapped in a kadre-core Window implementation
