package io.ygdrasil.koreos.uikit

import platform.UIKit.UIView
import platform.QuartzCore.CALayer

/**
 * Placeholder koreos-uikit — valide les imports cinterop K/N Apple (GRA-141).
 * Sera remplacé par l'implémentation réelle (GRA-142+).
 */
internal typealias UIViewAlias = UIView
internal typealias CALayerAlias = CALayer
