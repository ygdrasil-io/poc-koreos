name: invalid required workflow fixture

on:
  pull_request:
    paths:
      - "kadre/**"

jobs:
  host-contracts:
    runs-on: ubuntu-latest
    continue-on-error: true
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - run: echo "missing required host command"

  cross-platform-correctness:
    runs-on: ubuntu-latest
    needs: [host-contracts]
    steps:
      - run: echo "aggregate without required status checks"
