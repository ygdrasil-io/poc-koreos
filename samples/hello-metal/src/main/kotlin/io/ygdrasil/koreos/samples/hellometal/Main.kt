/**
 * Point d'entrée du sample hello-metal (Jalon M1).
 *
 * Ce squelette sera complété dans les tickets M1 dédiés une fois que
 * l'implémentation AppKit (koreos-appkit) sera disponible.
 * Il garantit qu'aucun avertissement "no source roots" n'est émis par Gradle.
 *
 * Usage : ./gradlew :samples:hello-metal:run
 * Prérequis : macOS avec JDK 25.
 */
package io.ygdrasil.koreos.samples.hellometal

/**
 * Démarre l'application hello-metal.
 *
 * TODO M1 : instancier EventLoop().runApp(HelloMetalApp())
 *           une fois l'implémentation koreos-appkit disponible.
 */
fun main() {
    println("hello-metal : squelette prêt — implémentation M1 à venir")
}
