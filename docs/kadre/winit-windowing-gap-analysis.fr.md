# Gap analysis fenetrage Kadre vs winit

Date: 2026-06-02
Commit winit cible: `c4afadbfabf7b1e7989b40b493db1a4c7bd8ff4e`
Portee: `WindowAttributes`, `Window`, `ActiveEventLoop`, types monitor/fullscreen/window support.
Hors portee: clavier, pointeur pur, IME riche sauf les methodes `Window` necessaires au pilotage IME.

References locales:

- winit: `third_party/winit/winit-core/src/window.rs`, `third_party/winit/winit-core/src/event_loop/mod.rs`, `third_party/winit/winit-core/src/monitor.rs`.
- Kadre: `kadre-core/src/commonMain/kotlin/org/graphiks/kadre/core/WindowAttributes.kt`, `Window.kt`, `ActiveEventLoop.kt`, `Monitor.kt`, `WindowingTypes.kt`.
- Garde-fou test: `kadre-core/src/commonTest/kotlin/org/graphiks/kadre/core/WinitWindowingCompatibilityTest.kt`.

## Synthese

Kadre couvre deja le socle portable de fenetrage winit: attributs de creation, creation de fenetre depuis `ActiveEventLoop`, controle de boucle, identite/handles, redraw, titre, tailles, scale factor, monitoring de base, fullscreen, theme/cursor/icon et quelques result types.

Les ecarts prioritaires ne demandent pas de breaking change dans cette tache, mais doivent rester visibles:

1. `Window.is_visible` et `Window.is_minimized` sont `Option<bool>` dans winit; Kadre les represente maintenant par `Boolean?`, avec `null` pour l'etat inconnu ou non expose par la plateforme.
2. Les operations winit fallibles de curseur et de gestion fenetre (`set_cursor_position`, `set_cursor_grab`, `drag_window`, `drag_resize_window`, `show_window_menu`) retournent maintenant des resultats types cote API commune; plusieurs backends doivent encore remplacer le fallback `RequestError.Unsupported` par du support natif.
3. `Window.available_monitors` et `Window.primary_monitor` existent dans winit en plus des methodes `ActiveEventLoop`; Kadre expose maintenant les equivalents `Window.availableMonitors()` et `Window.primaryMonitor()` avec fallback `emptyList`/`null` quand le backend ne sait pas repondre.
4. `ActiveEventLoop.owned_display_handle` est non nullable dans winit; `ActiveEventLoop.ownedDisplayHandle()` est nullable et retourne `null` par defaut dans Kadre.
5. `Window.request_ime_update` et `Window.ime_capabilities` sont reportes: l'IME riche est hors portee de ce passage, au-dela des methodes Kadre existantes `setImeAllowed`, `setImeCursorArea`, `setImePurpose`.
6. Plusieurs setters d'apparence et de gestion de fenetre existent cote API commune, mais restent incomplets backend par backend.

## Matrice cible

Statuts autorises par le test commun: `implemented`, `unsupported-platform`, `deferred`.

| API winit cible | API Kadre locale | Statut | Notes |
|---|---|---:|---|
| `WindowAttributes` | `WindowAttributes` | implemented | Couvre titre, taille, visibilite initiale, resizable, contraintes, position, fullscreen, decorations, activation, cursor, theme, transparence, level, icon, parent. |
| `Window.id` / handles / redraw / title / sizes / scale | `Window.id`, `rawWindowHandle`, `rawDisplayHandle`, `requestRedraw`, `title`, `innerSize`, `outerSize`, `scaleFactor` | implemented | Socle portable represente; X11 ecrit maintenant aussi `_NET_WM_NAME` en `UTF8_STRING` comme winit, en plus de `WM_NAME`. |
| Surface geometry et contraintes | `surfacePosition`, `outerPosition`, `setOuterPosition`, `surfaceSize`, `requestSurfaceSize`, `outerSize`, `safeArea`, `setMinSurfaceSize`, `setMaxSurfaceSize`, `surfaceResizeIncrements`, `setSurfaceResizeIncrements` | implemented | API portable presente; X11 honore maintenant `position` a la creation via `XCreateSimpleWindow`, lit `surfaceSize` via `XGetGeometry`, calcule `outerSize` avec `_NET_FRAME_EXTENTS`, redimensionne via `XResizeWindow`, suit les `ConfigureNotify` synthetiques root-relative et convertit via `_NET_FRAME_EXTENTS` pour `WindowEvent.Moved` quand les extents sont disponibles, et publie `WM_NORMAL_HINTS` pour min/max/increments/resizable; Wayland applique maintenant `requestSurfaceSize` immediatement avec `SurfaceSizeRequestResult.Applied` uniquement lorsque le dernier `xdg_toplevel.configure` est stateless comme winit, retourne sinon la taille courante controlee par le compositor, redemande un redraw apres changements min/max/resizable applicables, puis stocke les resize increments et les applique aux configure sizes comme winit; support exact encore variable selon backend. |
| `Window.is_visible() -> Option<bool>` | `Window.isVisible: Boolean?` | implemented | Kadre represente `Option<bool>` via un Boolean nullable; `null` signifie etat inconnu ou non expose par la plateforme. Wayland ignore maintenant `setVisible` au runtime et conserve `isVisible == null` comme winit. X11 suit maintenant la semantique winit `YesWait` jusqu a `VisibilityNotify`, emet aussi `WindowEvent.Occluded`, et remonte la fenetre via `XRaiseWindow` apres `XMapWindow` lors de `setVisible(true)`. |
| `Window.is_minimized() -> Option<bool>` | `Window.isMinimized: Boolean?` | implemented | Kadre represente `Option<bool>` via un Boolean nullable; `null` signifie etat inconnu ou non expose par la plateforme. X11 lit maintenant `_NET_WM_STATE_HIDDEN` comme winit quand disponible. |
| Monitor/fullscreen sur `Window` | `currentMonitor`, `setFullscreen`, `fullscreen` | implemented | Sous-ensemble portable present; X11 selectionne maintenant `currentMonitor` par plus grande intersection entre le rectangle de fenetre et les rectangles moniteurs comme winit, lit `_NET_WM_STATE_MAXIMIZED_VERT` et `_NET_WM_STATE_MAXIMIZED_HORZ` pour `isMaximized`, comme winit, et differe les requetes fullscreen tant que la fenetre est `No`/`YesWait` avant application sur `VisibilityNotify`. |
| Focus sur `Window` | `focusWindow`, `hasFocus` | implemented | API commune presente; AppKit et Win32 implementent le focus avec la garde winit `visible && !minimized && !foreground` quand applicable; X11 envoie maintenant `_NET_ACTIVE_WINDOW` avec la garde winit `visible && !minimized`, lit `WM_STATE`/`IconicState` pour cette garde, met a jour `hasFocus` via FocusIn/FocusOut, et corrige les offsets Xlib LP64 pour router les events vers le bon `WindowId`. Wayland met maintenant a jour `hasFocus` depuis les transitions `wl_keyboard.enter`/`leave`, avec comptage par seat comme winit. Les autres backends restent a valider finement. |
| Apparence et etat fenetre | `setWindowLevel`, `requestUserAttention`, `setTheme`, `theme`, `setTransparent`, `setBlur`, `setWindowIcon`, `setContentProtected` | deferred | API commune presente; `requestUserAttention` et `setContentProtected` retournent maintenant `WindowRequestResult`, avec AppKit attention/protection cablees, Win32 attention/protection cablees, X11/Wayland content protection success no-op comme winit, et `RequestError.Unsupported` par defaut ailleurs. AppKit, Win32 et X11 appliquent aussi `windowLevel` depuis les attributs initiaux; Wayland garde `setWindowLevel` en no-op comme winit car le Z-order est gere par le compositor; AppKit applique maintenant `WindowAttributes.transparent` et `WindowAttributes.blur` a la creation avant affichage, `setTransparent` bascule aussi `NSColor.clearColor` / `NSColor.windowBackgroundColor` comme winit, et `setBlur` est idempotent/desactivable en retirant la vue de blur inseree. Win32 applique maintenant aussi `WindowAttributes.transparent` avant `ShowWindow` via le chemin transparent Kadre (`WS_EX_LAYERED` + `SetLayeredWindowAttributes`) et active le blur-behind DWM initial avec une region vide comme winit quand `DwmEnableBlurBehindWindow` est disponible. Wayland stocke maintenant `preferredTheme`/`setTheme`, retourne `theme` comme l'etat CSD winit, et `setContentProtected` retourne success no-op comme winit faute de protocole capture-protection portable; detection portail systeme toujours absente. Wayland met maintenant a jour `isMaximized` et `fullscreen` depuis les states confirmes `xdg_toplevel.configure` comme winit, sans mutation optimiste dans les setters. Wayland conserve maintenant le hint `transparent` a la creation et au runtime, emet `wl_surface.set_opaque_region(NULL)` pour transparent ou une region opaque pleine quand les proxies `wl_region` sont disponibles, et cable les decorations initiales/runtime via `zxdg_toplevel_decoration_v1.set_mode` quand le compositor expose xdg-decoration; l'effet visuel depend toujours des buffers alpha du renderer/compositor. X11 applique maintenant aussi `WindowAttributes.decorations` a la creation via `_MOTIF_WM_HINTS`, preserve les champs Motif existants lors de `setDecorations`, `setResizable` met a jour le hint Motif `MAXIMIZE` comme winit, et `requestUserAttention` bascule le flag urgent de `WM_HINTS` comme winit; `setTheme` ecrit `_GTK_THEME_VARIANT` comme winit tout en gardant `theme == null`, `setTransparent` et `setBlur` restent des no-op comme winit local, et `setContentProtected` retourne success no-op comme winit faute de mecanisme X11 portable. `setWindowIcon` est cable sur Win32 (`WM_SETICON/ICON_SMALL`) et X11 (`_NET_WM_ICON`), tandis qu'AppKit reste volontairement no-op comme winit faute d'icone de fenetre native. AppKit et Win32 câblent aussi `enabledButtons`; X11/Wayland restent alignes sur winit avec `WindowButtons.ALL`/non implemente. Les autres setters apparence restent a auditer pour supprimer les no-op silencieux. |
| `Window.reset_dead_keys()` | `resetDeadKeys()` | implemented | API commune presente avec implementations ou no-op best-effort documentes par plateforme. |
| `Window.available_monitors()` | `Window.availableMonitors()` | implemented | Methode Window-level presente; fallback commun `emptyList()` quand inconnu; overrides desktop ou synthetiques quand l'enumeration backend est disponible. |
| `Window.primary_monitor()` | `Window.primaryMonitor()` | implemented | Nullable comme winit; fallback commun `null` quand inconnu; overrides quand le backend connait un primaire. X11 utilise le primaire XRandR quand disponible; Wayland retourne `null` par absence de concept primaire. |
| Cursor grab/position/hittest | `setCursor`, `setCursorVisible`, `setCursorGrab`, `setCursorPosition`, `setCursorHittest` | implemented | `setCursorGrab`, `setCursorPosition` et `setCursorHittest` retournent `WindowRequestResult`; les backends sans support réel retournent `Failure(RequestError.Unsupported(...))`. `setCursor` et `setCursorVisible` restent des setters `Unit` no-throw; X11 applique maintenant le curseur initial, memorise le curseur selectionne comme winit, cache le curseur via un curseur pixmap transparent 1x1, et cable `setCursorHittest` via l'input shape X11 (`XShapeCombineRectangles`). Wayland accepte maintenant `CursorGrabMode.None` en success no-op comme winit quand `zwp_pointer_constraints_v1` est absent, garde `Confined`/`Locked` en `RequestError.Unsupported`, cable aussi `setCursorHittest` via `wl_surface.set_input_region`, avec `NULL` pour hittest active et une region vide pour click-through comme winit; `setCursorVisible(false)` cache le curseur via `wl_pointer.set_cursor(NULL)` sur le dernier enter serial, tandis que la restauration visible attend encore une surface de curseur themee. |
| `Window.drag_window()` | `Window.dragWindow()` | deferred | Kadre retourne `WindowRequestResult` au lieu d'un no-op `Unit`; AppKit marshalise vers la main queue puis utilise le `NSEvent` courant et retourne `RequestError.Ignored` quand aucun evenement courant n'est disponible; Win32 enfile les appels cross-thread vers le thread fenetre; X11 envoie `_NET_WM_MOVERESIZE`; Wayland envoie `xdg_toplevel.move`; le demarrage natif reste fire-and-forget. |
| `Window.drag_resize_window()` | `Window.dragResizeWindow()` | deferred | Kadre retourne `WindowRequestResult` au lieu d'un no-op `Unit`; Win32 enfile les appels cross-thread vers le thread fenetre; X11 envoie `_NET_WM_MOVERESIZE`; Wayland envoie `xdg_toplevel.resize`; AppKit est unsupported comme winit; le demarrage natif reste fire-and-forget. |
| `Window.show_window_menu()` | `Window.showWindowMenu()` | deferred | Kadre retourne `WindowRequestResult` au lieu d'un no-op `Unit`; le menu systeme Win32 est cable; Wayland envoie `xdg_toplevel.show_window_menu` avec le dernier serial pointeur; AppKit et X11 retournent maintenant success no-op comme winit local. |
| `Window.request_ime_update()` | `setImeAllowed`, `setImeCursorArea`, `setImePurpose` | deferred | IME riche hors portee, sauf methodes Window existantes. |
| `Window.ime_capabilities()` | absent | deferred | Kadre n'a pas le modele de capacites IME winit. |
| `ActiveEventLoop` create/control/exit/proxy | `createWindow`, `controlFlow`, `setControlFlow`, `exit`, `isExiting`, `createProxy` | implemented | Socle de controle represente. |
| `ActiveEventLoop.available_monitors()` | `availableMonitors()` | implemented | Equivalent local. |
| `ActiveEventLoop.primary_monitor()` | `primaryMonitor()` | implemented | Nullable comme winit. |
| `ActiveEventLoop.owned_display_handle()` | `ownedDisplayHandle(): OwnedDisplayHandle?` | deferred | winit non-null; Kadre nullable avec `null` par defaut. |
| `MonitorHandle`, `VideoMode` | `MonitorHandle`, `VideoMode` | implemented | Id, nom, position, scale factor, mode courant, modes disponibles. |
| `Fullscreen::{Borderless, Exclusive}` | `Fullscreen.Borderless`, `Fullscreen.Exclusive` | unsupported-platform | API presente, mais `Exclusive` est documente comme fallback/no-op sur Wayland, Web, Android, UIKit. |
| Result/error support | `WindowRequestResult`, `SurfaceSizeRequestResult`, `RequestError` | implemented | Types utilises par `requestSurfaceSize`, les requetes curseur fallibles (`setCursorGrab`, `setCursorPosition`, `setCursorHittest`), les requetes de gestion fenetre (`showWindowMenu`, `dragWindow`, `dragResizeWindow`) et les requetes diverses (`requestUserAttention`, `setContentProtected`), avec `RequestError.Ignored` pour les requetes ignorees par la plateforme. |

## Implications API

Cette analyse suit l'API courante. Les methodes Window-level de monitoring sont maintenant presentes avec un fallback portable `emptyList`/`null` documente.

Priorites probables pour une suite:

1. Completer les backends desktop qui exposent encore des etats best-effort pour documenter ou tester leur source native.
2. Convertir les operations cursor/drag critiques vers des result types Kadre existants, sans exceptions pour les limitations attendues.
3. Faire progresser les backends non desktop vers une enumeration multi-ecran reelle si la plateforme expose plus qu'un moniteur synthetique.
4. Rendre `ownedDisplayHandle` non nullable quand chaque backend peut produire un handle fiable, ou documenter la divergence.
5. Reporter `request_ime_update` / `ime_capabilities` vers un ticket IME dedie.
6. Faire progresser les backends desktop un par un pour les setters deja exposes: focus, content protection, resize increments, attention utilisateur, window level, theme et icon.
