# Task 6 — CI, documentation, and closure evidence

## Commit

- `7753590912df914622f1bf6745173dea86b40443` — `ci: enforce cross-platform correctness gates`

## Files

- Added `.github/workflows/cross-platform-correctness.yml` and `.github/fixtures/workflow-contract-invalid.yml`.
- Updated `.github/workflows/ci.yml`, `linux-compose-visual.yml`, `android-visual.yml`, `ios-visual.yml`, and `macos-visual.yml` to remove success masking from required checks and cap jobs at 25 minutes.
- Marked the pre-existing Windows/Linux GPU-readback jobs as explicitly optional hardware diagnostics, outside the aggregate; adjusted `web-e2e.yml` so it no longer masks a missing Chrome application.
- Added `scripts/verify-test-results.py`, `scripts/check-workflow-contract.py`, `scripts/test-workflow-contract.sh`, `scripts/android-emulator-test.sh`, `scripts/test-linux-container.sh`, and the 18-row report fixture.
- Updated `scripts/test-web-browsers.sh` to invoke central JUnit validation, and removed broad masking forms from supporting scripts so the required audit has meaningful output.
- Added `docs/kadre/cross-platform-correctness-report.md`; updated `CHANGELOG.md`, both specs, and both Linux tutorials.

## Tests and observed results

- RED fixture: before the report-numbering fix, `rtk bash scripts/test-workflow-contract.sh` failed because the validator counted zero numbered rows instead of the expected 18. The regex was corrected immediately afterwards.
- `rtk scripts/test-workflow-contract.sh` exited 0. It rejects the workflow fixture for its path filter, `continue-on-error`, timeout, and missing script-owned command; it rejects the 18-row report fixture for exactly the missing nineteenth row; it accepts the real workflow and 19-row report.
- `rtk python3 scripts/verify-test-results.py --help` exited 0.
- `rtk python3 scripts/verify-test-results.py --junit kadre/build/test-results/jvmTest --png tests/visual/baselines/linux/hello-triangle.png --report docs/kadre/cross-platform-correctness-report.md` exited 0 with `tests=12 skipped=0 failures=0 errors=0`, a decoded `800x600` foreground PNG, and 19 complete report rows.
- `rtk ruby -e "require 'yaml'; ..."` parsed all six changed/created required workflow YAML files successfully; `rtk bash -n` and `rtk python3 -m py_compile` passed for the new and changed validators/scripts.
- `rtk ./gradlew :kadre-test:allTests :kadre:jvmTest :kadre-x11:jvmTest :kadre-wayland:jvmTest :kadre-appkit:jvmTest :kadre-web-common:allTests --no-daemon --console=plain --quiet` exited 0.
- `rtk git diff --check` was clean before the implementation commit.

## CI contract

- PR-only `cross-platform-correctness.yml` has six non-masked matrix jobs: host contracts, JS/Wasm browser contracts, UIKit simulator contracts, Android emulator contracts, glibc/musl Linux X11/Wayland container contracts, and deterministic raster capture.
- Every required job has a timeout of at most 25 minutes and runs a script-owned validator or test command. The Android and deterministic capture paths decode PNGs through `verify-test-results.py`; browser results receive central JUnit validation.
- The `cross-platform-correctness` aggregate uses `if: always()`, needs every matrix job, and explicitly requires each `needs.<job>.result` to equal `success`; no optional hardware diagnostic is a dependency.
- `check-workflow-contract.py` rejects missing matrix jobs, path filters, any `continue-on-error` or `||` masking in a required job, a timeout above 25 minutes, missing script-owned commands, and a missing aggregate dependency/status check.

## Constraints and risks

- The actual iOS simulator, Android emulator, Docker glibc/musl, browser, and GitHub Actions artifact runs were not performed locally in this task. The evidence document labels them as planned commands or expected CI artifacts, not completed runs.
- The Linux container script uses the `gradle:9.2.1-jdk25` and `gradle:9.2.1-jdk25-alpine` images; image availability and package names remain an external CI dependency.
- The remaining `continue-on-error` / `|| true` audit matches are confined to `windows-visual.yml`, `linux-visual.yml`, and `linux-x11-visual.yml`, whose only jobs are explicitly named optional hardware diagnostics and are not part of the aggregate.
- `kotlin-js-store/wasm/` was generated by the local Gradle run, deliberately excluded from the commits, then removed during cleanup.

## Fix round 1 — independent-review remediation

### Commits

- `f13c846` — `ci: harden evidence contract validation`
- `f9a0385` — `ci: install structured workflow parser`

### Corrections

- `check-workflow-contract.py` now loads YAML structurally with PyYAML `BaseLoader`, preserving the GitHub `on` key while ignoring comments. It verifies a real unfiltered `pull_request` trigger, the exact ordered `needs` list, aggregate `if: always()`, executable `[[ "${{ needs.<job>.result }}" == "success" ]]` assertions, timeout bounds, and real `run`/action `with.script` commands rather than substrings.
- Four negative YAML fixtures cover command text held only in a comment, a `true` no-op command, `needs: []`, and a missing `always()` condition. The workflow installs the pinned `PyYAML==6.0.3` dependency before its host contract runs.
- `verify-test-results.py` now recursively compares every declared JUnit suite counter with actual descendant `<testcase>`, `<skipped>`, `<failure>`, and `<error>` elements. Fixtures cover a declared test with no testcase, a skipped-element mismatch, and a valid nested suite.
- PNG validation now requires `--png-target`. `compose-raster`, `android-triangle`, and `hello-triangle-baseline` each document/enforce 800×600 dimensions, color diversity, and a target-specific non-background visible region derived from the most frequent normalized RGBA pixel rather than a black-background assumption.
- PNG fixtures cover a 1×1 image, an 800×600 solid image, and an 800×600 image with only eight changed pixels.

### Validation

- `rtk scripts/test-workflow-contract.sh` exited 0 after rejecting every new negative fixture and accepting the real workflow/report; it also accepted the two-case nested JUnit fixture.
- `rtk python3 scripts/verify-test-results.py --junit kadre-web-common/build/test-results/jsBrowserTest --junit kadre-web-common/build/test-results/wasmJsBrowserTest --png tests/visual/baselines/linux/hello-triangle.png --png-target hello-triangle-baseline --report docs/kadre/cross-platform-correctness-report.md` exited 0: 282 tests, zero skips/failures/errors; baseline 800×600 with 47,436 colors and 60,000 non-background pixels; 19 report rows.
- `rtk python3 scripts/verify-test-results.py --junit kadre-uikit/build/test-results/iosSimulatorArm64Test` exited 0: 60 tests, zero skips/failures/errors.
- Python compilation, Bash syntax, Ruby YAML parsing of the six required workflows, and `rtk git diff --check` passed before the fix commits.

### Remaining risks

- The iOS simulator, Android emulator, browser, Docker glibc/musl, and GitHub Actions artifact paths remain external CI executions and were not claimed as local runs.
- The host contract now downloads a pinned PyYAML wheel/package through pip; availability of the package index is an external CI dependency.

## Fix round 2 — canonical shell-step enforcement

### Commit

- `ab6fa25c4409bdee0b9624df804276db8733502d` — `ci: require canonical correctness steps`

### Corrections

- The structured YAML checker no longer treats a shell fragment as evidence that it was executed. For every required job it accepts only a complete, whitespace-normalized canonical `run` string (or the Android action's exact `with.script` input); it accepts each aggregate result assertion only as its own complete canonical `run` string.
- The aggregate workflow was rewritten into six one-command assertion steps. Because matching is full-string equality after whitespace normalization, a command/assertion placed in `if false`, a comment, `true`, a pipeline, a sub-shell, or any other control block cannot satisfy the contract.
- Added negative fixtures for the required host command hidden in `if false` and the host aggregate success assertion hidden in `if false`; `scripts/test-workflow-contract.sh` proves both are rejected. This is a local checker proof, not a claim that the hosted CI ran.

### Validation

- RED evidence produced before the checker change: `rtk bash scripts/test-workflow-contract.sh` rejected neither new `if false` fixture, demonstrating the original regex false positive.
- GREEN local evidence: `rtk scripts/test-workflow-contract.sh` exited 0, rejected all negative workflow/JUnit/PNG/report fixtures including both `if false` cases, and accepted the real workflow and 19-row evidence report.
- `rtk python3 scripts/verify-test-results.py --junit kadre-web-common/build/test-results/jsBrowserTest --junit kadre-web-common/build/test-results/wasmJsBrowserTest --png tests/visual/baselines/linux/hello-triangle.png --png-target hello-triangle-baseline --report docs/kadre/cross-platform-correctness-report.md` exited 0: 282 tests, zero skips/failures/errors; PNG 800×600, 47,436 colors, 60,000 non-background pixels; 19 rows.
- `rtk python3 scripts/verify-test-results.py --junit kadre-uikit/build/test-results/iosSimulatorArm64Test` exited 0: 60 tests, zero skips/failures/errors. Python compilation, Bash syntax, Ruby YAML parsing of the real six relevant workflows, and `rtk git diff --check` also passed.

### Remaining risks

- Hosted iOS, Android, browser, Docker glibc/musl, and artifact checks remain expected CI executions rather than local evidence. The canonical checker validates workflow structure only; it does not execute GitHub Actions or parse arbitrary shell.
- The checker deliberately permits only the documented canonical command forms, so a legitimate future command change must update the workflow contract and its fixtures together. PyYAML package-index availability remains an external CI dependency.

## Fix round 3 — Android runner-step binding

### Commit

- `65a4b715683463fc5daed0fe08ca345415f3cce7` — `ci: bind Android runner contract`

### Corrections

- The Android required-job contract now validates one complete structured step: `uses` must equal `reactivecircus/android-emulator-runner@v2` and that same step's `with.script` must equal the whitespace-normalized `scripts/android-emulator-test.sh` command. No SHA variant is admitted by this contract.
- Added `workflow-contract-android-noop-action.yml`, which supplies the expected `with.script` to `actions/checkout@v4`. The checker rejects it with `android-emulator-contracts: missing canonical Android emulator runner step`, proving that a no-op action cannot satisfy the Android requirement.

### Validation

- RED evidence produced before this correction: `rtk bash scripts/test-workflow-contract.sh` failed because the no-op Android fixture lacked no Android-contract error, even though it contained the expected script field.
- GREEN local evidence: direct validation of the fixture reported `missing canonical Android emulator runner step`; `rtk scripts/test-workflow-contract.sh` then exited 0, rejecting it alongside all prior negative fixtures and accepting the actual workflow/report.
- `rtk python3 scripts/check-workflow-contract.py .github/workflows/cross-platform-correctness.yml`, `rtk python3 -m py_compile scripts/check-workflow-contract.py`, `rtk bash -n scripts/test-workflow-contract.sh`, and `rtk git diff --check` all exited 0.

### Remaining risks

- Android emulator execution itself remains an expected GitHub Actions run, not local evidence. The contract deliberately pins the accepted action reference to `reactivecircus/android-emulator-runner@v2`; a future SHA pin must be explicitly designed, documented, and covered by fixtures before it is admitted.

## Fix round 4 — unconditional proof steps

### Commit

- `ffb6f7733131578c7e2acec705cf6223f30e502f` — `ci: reject conditional proof steps`

### Corrections

- A required `run`, an aggregate success assertion, and the Android `uses` + `with.script` proof now count only when their step has no GitHub Actions `if` key. The checker rejects the key regardless of its value, covering `if: false`, `if: ${{ false }}`, and any other step-level condition.
- Added three negative fixtures: a host canonical `run` with `if: false`, an aggregate host-success assertion with `if: ${{ false }}`, and the canonical Android runner step with `if: false`. The real workflow retains no `if` on proof steps and remains accepted.

### Validation

- RED evidence produced before this correction: `rtk bash scripts/test-workflow-contract.sh` failed at `workflow-contract-command-step-if.yml`, because the conditional host step was still accepted as required-command evidence.
- GREEN local evidence: `rtk scripts/test-workflow-contract.sh` exited 0 and rejected all three conditional-step fixtures with their targeted errors, together with every earlier negative fixture; it accepted the real workflow and the 19-row evidence report.
- `rtk python3 scripts/check-workflow-contract.py .github/workflows/cross-platform-correctness.yml`, `rtk python3 -m py_compile scripts/check-workflow-contract.py`, `rtk bash -n scripts/test-workflow-contract.sh`, and `rtk git diff --check` all exited 0.

### Remaining risks

- The checker validates GitHub Actions YAML structure and intentionally cannot establish that hosted actions execute correctly. iOS, Android, browser, Docker glibc/musl, and artifact paths remain expected CI executions, not local evidence.

## Fix round 5 — proof-step execution context

### Commit

- `dc0e6c5d0ce273aab2324b2f5f67ab37a10a97db` — `ci: harden proof step execution`

### Corrections

- Every required `run` and aggregate assertion now rejects proof steps carrying a `shell` key, so a custom shell such as `shell: echo {0}` cannot turn a canonical command into a no-op. The same proof-step predicate rejects `working-directory`, preserving repository-root interpretation of relative script paths.
- The Android proof step also rejects `working-directory`; it continues to require the exact emulator-runner action and canonical `with.script` in the same unconditional step.
- Added negative fixtures for a required host command and aggregate assertion using `shell: echo {0}`, plus `working-directory` fixtures for a host required command and the Android runner. The real workflow has none of these keys on proof steps and remains accepted.

### Validation

- RED evidence produced before this correction: `rtk bash scripts/test-workflow-contract.sh` failed at `workflow-contract-command-step-shell.yml`, showing that the custom-shell host proof was previously accepted.
- GREEN local evidence: `rtk scripts/test-workflow-contract.sh` exited 0 and rejected all four new `shell`/`working-directory` fixtures, alongside all preceding negative fixtures, while accepting the actual workflow and the 19-row evidence report.
- `rtk python3 scripts/check-workflow-contract.py .github/workflows/cross-platform-correctness.yml`, `rtk python3 -m py_compile scripts/check-workflow-contract.py`, `rtk bash -n scripts/test-workflow-contract.sh`, and `rtk git diff --check` all exited 0.

### Remaining risks

- This fifth authorized correction is intentionally limited to the reviewer-identified execution-context bypass. Further Critical findings require the planned adjudication rather than another iterative corrective loop. Hosted iOS, Android, browser, Docker glibc/musl, and artifact checks remain expected CI executions, not local evidence.
