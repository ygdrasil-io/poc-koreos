/**
 * Squelette du module koreos (façade publique).
 *
 * Ce fichier est un placeholder qui sera remplacé par les déclarations
 * expect/actual déléguant vers les backends spécifiques (koreos-appkit,
 * koreos-uikit M3, koreos-android M3).
 * Il garantit qu'aucun avertissement "no source roots" n'est émis par Gradle.
 *
 * Cibles : commonMain (jvm, androidTarget, iosX64, iosArm64, iosSimulatorArm64).
 */
package io.ygdrasil.koreos

// TODO M1 : expect class EventLoop — actual jvmMain délégue vers koreos-appkit
// TODO M3 : actual iosMain délégue vers koreos-uikit
// TODO M3 : actual androidMain délégue vers koreos-android
