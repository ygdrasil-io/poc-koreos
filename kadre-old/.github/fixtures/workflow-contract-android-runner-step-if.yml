name: conditional Android runner workflow fixture

on:
  pull_request:

jobs:
  android-emulator-contracts:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - if: false
        uses: reactivecircus/android-emulator-runner@v2
        with:
          script: scripts/android-emulator-test.sh
