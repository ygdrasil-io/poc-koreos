name: true-command workflow fixture

on:
  pull_request:

jobs:
  host-contracts:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - run: true

  cross-platform-correctness:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    if: always()
    needs: []
    steps:
      - run: true
