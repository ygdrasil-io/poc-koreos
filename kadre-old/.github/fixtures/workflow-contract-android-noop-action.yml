name: Android no-op action workflow fixture

on:
  pull_request:

jobs:
  android-emulator-contracts:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
        with:
          script: scripts/android-emulator-test.sh
